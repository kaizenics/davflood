{
  "name": "davflood",
  "private": true,
  "version": "1.2.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "dev": "pnpm -F web dev",
    "build": "pnpm -F web build",
    "check-types": "pnpm -r check-types",
    "validate:style": "pnpm -F @davflood/hazard validate:style"
  },
  "devDependencies": {
    "@davflood/config": "workspace:*",
    "@types/node": "^22.20.1",
    "typescript": "^6.0.3"
  },
  "packageManager": "pnpm@10.30.0"
}
