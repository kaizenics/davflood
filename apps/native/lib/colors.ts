/**
 * Raw colour strings for the places React Native needs a value rather than a
 * class: navigator options, vector-icon `color` props, MapLibre paint.
 *
 * Re-exported from the shared package so the app, the marketing site and the
 * map style all resolve to the same bytes.
 */
export { colors } from "@naboflood/hazard/tokens";
export type { ColorToken } from "@naboflood/hazard/tokens";
